[Export]
baked_state = false
offer_binder = true
slider_server_url="https://mattborghi.github.io/pluto-deploy"
ignore_cache = []
create_index = false

[SliderServer]
host = "0.0.0.0"
port = 1234
exclude=[
    # these are in the repo but not used on the website
]
